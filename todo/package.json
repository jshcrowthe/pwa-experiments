{
  "name": "todo",
  "version": "1.0.0",
  "description": "Todo as a PWA",
  "main": "index.html",
  "scripts": {
    "install": "bower install",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "bower install && sw-precache --root=. --static-file-globs='./**/*.@(js|html|css)'"
  },
  "author": "Josh Crowther <jshcrowthe@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "bower": "^1.7.9",
    "gulp": "^3.9.1",
    "sw-precache": "^3.2.0"
  }
}
